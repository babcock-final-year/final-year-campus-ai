{
	"name": "@apps/backend",
	"private": true,
	"scripts": {
		"build": "echo 'No build for server code. Skipping...'",
		"check": "bun run format:check && bun run lint",
		"clean": "rm -rf .ruff_cache .pytest_cache __pycache__ .mypy_cache",
		"dev": "uv run python -m flask --app app run --host 0.0.0.0 --port 5000 --debug",
		"format": "uv run ruff format .",
		"format:check": "uv run ruff format . --check",
		"gen:types": "uv run ./gen-types.py",
		"lint": "uv run ruff check .",
		"lint:fix": "uv run ruff check . --fix",
		"pkg:install": "UV_VENV_CLEAR=1 uv venv && uv pip install -r requirements.txt",
		"run": "uv run python -m flask --app app run --host 0.0.0.0 --port 5000",
		"setup": "uv sync --dev",
		"setup:prod": "uv sync --no-dev",
		"start": "uv run python -m flask --app app run --host 0.0.0.0 --port ${PORT:-5000}",
		"test": "uv run python -m unittest discover -s . -p \"test*.py\""
	},
	"version": "0.0.0"
}
